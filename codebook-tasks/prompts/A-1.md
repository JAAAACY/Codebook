# Task: A-1 — 环境准备（clone 测试仓库）
# Wave: W1
# Project: CodeBook (mcp-server/)
# Working Dir: The repo root where files/CLAUDE.md exists

先读取以下文件，建立核心上下文：
1. files/CLAUDE.md — 全量读取（产品定义 + 技术栈约束 + 编码规范 + 禁止事项，~6K tokens）
2. files/CONTEXT.md — 全量读取（当前 sprint 进度 + 流水线状态 + 已知问题 + 待确认决策，~5K tokens）
3. files/INTERFACES.md — 只读 §2.1 scan_repo（其余跳过，节约上下文空间）

📊 上下文预算：文档 ~8K + 源码 ~0K + 产出 ~5K + 执行 ~2K = ~15K tokens (8%) 🟢

【任务】流水线 A / 环境准备
【所属】Wave 1（可与 C-1、D-1a 并行）
【前置】W0-1 对齐确认完成

执行步骤：
1. 按以下梯度 clone 到 mcp-server/repos/（每个 git clone --depth 1）：

   | 梯度 | 项目 | 语言 | 预估行数 | repo_url |
   |------|------|------|---------|---------|
   | 中型 | FastAPI | Python | ~1.5万 | https://github.com/tiangolo/fastapi |
   | 中大型 | Sentry Python SDK | Python | ~3万 | https://github.com/getsentry/sentry-python |
   | 大型 | Next.js | TypeScript | ~10万+ | https://github.com/vercel/next.js |
   | 超大型 | VS Code | TypeScript | ~50万+ | https://github.com/microsoft/vscode |

2. 对每个用 cloc（如无则 find+wc）统计
3. 每个写 test_results/{name}/stats.json：
   {"project": str, "repo_url": str, "clone_date": "YYYY-MM-DD", "file_count": N, "code_lines": N, "languages": {...}}
4. 验证：实际行数与预估差异 > 50% 的要标注原因
5. 更新 CONTEXT.md 测试项目梯度表

不跑 scan_repo。不改 src/。

═══ 质量自检清单（任务完成前逐项确认）═══

□ 1. 代码规范：所有新代码有类型注解 + structlog 日志 + 无 print
□ 2. 测试覆盖：新功能有正常路径 + 至少 1 个异常路径测试
□ 3. 回归验证：cd mcp-server && python -m pytest tests/ -x -q → 记录结果
□ 4. 接口一致：如修改了 tool 输入输出 → INTERFACES.md 已同步更新
□ 5. 错误处理：对外返回 {"status": "error", "error": str, "hint": str}
□ 6. 无硬编码：无 API Key / 模型名 / 绝对路径 硬编码
□ 7. 产出物完整：所有声称的产出文件确实存在且内容完整
□ 8. CONTEXT.md 已更新：追加任务日志（固定格式）
