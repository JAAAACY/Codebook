# Task: B-1 — 角色分析 + 新系统设计
# Wave: W2
# Project: CodeBook (mcp-server/)
# Working Dir: The repo root where files/CLAUDE.md exists

先读取以下文件，建立核心上下文：
1. files/CLAUDE.md — 全量读取（产品定义 + 技术栈约束 + 编码规范 + 禁止事项，~6K tokens）
2. files/CONTEXT.md — 全量读取（当前 sprint 进度 + 流水线状态 + 已知问题 + 待确认决策，~5K tokens）
3. files/INTERFACES.md — 只读 §2 Tool 契约, §3 角色系统（其余跳过，节约上下文空间）
4. docs/self_evolution_design.md — 只读 第二章：术语飞轮

📊 上下文预算：文档 ~23K + 源码 ~25K + 产出 ~15K + 执行 ~0K = ~63K tokens (32%) 🟡

【任务】流水线 B / 角色分析 + 新系统设计
【所属】Wave 2（可与 A-2、D-2a 并行）
【前置】W0-1 对齐确认完成

额外读取源码（grep 而非全量读——按需定位）：
- grep -r "ROLE_GUIDANCE\|ROLE_CONFIG\|role\|banned_terms" mcp-server/src/tools/ → 定位角色逻辑
- mcp-server/prompts/codebook_config_v0.2.json — 全量读取
- 每个 tool 中与角色相关的代码段

执行步骤：

1. 分析现有角色系统（完整审计）
   - 读取 prompts/codebook_config_v0.2.json
   - Grep 所有 tool 中 ROLE_GUIDANCE / ROLE_CONFIG / role / banned_terms
   - 列出每个 tool 对 role 参数的实际处理方式
   - 4 角色对比矩阵：哪些差异有意义/纯措辞/完全缺失

2. 收集需求（为每类用户设计 5 个典型使用场景）
   - 开发者：接手新项目、Review 代码、定位 bug、理解调用链、评估改动影响
   - PM/管理层：了解项目状态、评估功能复杂度、跨团队沟通、变更影响、排期参考
   - 行业专家：验证业务规则实现、检查合规性、审查领域逻辑、术语准确性、风险评估

3. 设计新角色系统
   - dev / pm / domain_expert 三视图的详细行为定义
   - 每种视图在每个 tool 输出中的具体差异（不只是概念，要到字段级别）
   - domain_expert 的 project_domain 传入机制（解答 D-004）：
     · 优先：scan_repo 输入参数显式传入
     · 兜底：README 关键词 + 依赖包名自动推断
     · 推断规则表（什么关键词/包名 → 什么 domain）
   - 向后兼容映射表
   - 与 TermResolver 的集成点

4. 写入 docs/role_system_v3_design.md

这个任务只做分析和设计，不写实现代码。
设计文档要具体到可以直接编码的程度。

═══ 质量自检清单（任务完成前逐项确认）═══

□ 1. 代码规范：所有新代码有类型注解 + structlog 日志 + 无 print
□ 2. 测试覆盖：新功能有正常路径 + 至少 1 个异常路径测试
□ 3. 回归验证：cd mcp-server && python -m pytest tests/ -x -q → 记录结果
□ 4. 接口一致：如修改了 tool 输入输出 → INTERFACES.md 已同步更新
□ 5. 错误处理：对外返回 {"status": "error", "error": str, "hint": str}
□ 6. 无硬编码：无 API Key / 模型名 / 绝对路径 硬编码
□ 7. 产出物完整：所有声称的产出文件确实存在且内容完整
□ 8. CONTEXT.md 已更新：追加任务日志（固定格式）
