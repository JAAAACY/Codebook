# Task: A-3 — read_chapter + diagnose 压测
# Wave: W3
# Project: CodeBook (mcp-server/)
# Working Dir: The repo root where files/CLAUDE.md exists

先读取以下文件，建立核心上下文：
1. files/CLAUDE.md — 全量读取（产品定义 + 技术栈约束 + 编码规范 + 禁止事项，~6K tokens）
2. files/CONTEXT.md — 全量读取（当前 sprint 进度 + 流水线状态 + 已知问题 + 待确认决策，~5K tokens）
3. files/INTERFACES.md — 只读 §2.2 read_chapter, §2.3 diagnose（其余跳过，节约上下文空间）

📊 上下文预算：文档 ~10K + 源码 ~10K + 产出 ~20K + 执行 ~20K = ~60K tokens (30%) 🟡

【任务】流水线 A / read_chapter + diagnose 压力测试
【所属】Wave 3（可与 B-2a、D-3 并行）
【前置】A-2 已完成

额外读取：test_results/scan_repo_summary.md（了解各项目模块分布）

对每个成功完成 scan_repo 的项目：

read_chapter 测试：
1. 从 modules 选 3 个模块（小<500行, 中500-2000行, 大>2000行）
2. 每个运行 read_chapter
3. 记录：
   - 响应时间
   - module_cards 数组长度 vs 模块实际文件数
   - functions 列表是否完整（与 scan_repo 的 source_refs 交叉验证）
   - calls 调用关系是否准确（抽查 3 个）
   - 是否触发 pagination
   - 翻译质量（PM 视角 1-10，扣分点具体写出来）

diagnose 测试：
4. 为每个项目设计 2 个场景：
   - 场景 1：跨 3+ 文件的调用链追踪
   - 场景 2：需要理解业务逻辑的问题
5. 运行 diagnose（参数是 query）
6. 记录：
   - keywords 提取 vs 人工预期的关键词对比
   - matched_nodes 命中数和 score 分布
   - exact_locations 人工抽查 3 个：file/line 是否准确、direction 是否合理
   - call_chain Mermaid 是否连贯（有没有跳过中间节点）
   - context 文本是否包含足够信息（如果你是宿主 LLM，能否基于此推理出答案）

═══ 质量自检清单（任务完成前逐项确认）═══

□ 1. 代码规范：所有新代码有类型注解 + structlog 日志 + 无 print
□ 2. 测试覆盖：新功能有正常路径 + 至少 1 个异常路径测试
□ 3. 回归验证：cd mcp-server && python -m pytest tests/ -x -q → 记录结果
□ 4. 接口一致：如修改了 tool 输入输出 → INTERFACES.md 已同步更新
□ 5. 错误处理：对外返回 {"status": "error", "error": str, "hint": str}
□ 6. 无硬编码：无 API Key / 模型名 / 绝对路径 硬编码
□ 7. 产出物完整：所有声称的产出文件确实存在且内容完整
□ 8. CONTEXT.md 已更新：追加任务日志（固定格式）
