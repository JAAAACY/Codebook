# Task: D-4 — 智能记忆 + 隐式推断
# Wave: W4
# Project: CodeBook (mcp-server/)
# Working Dir: The repo root where files/CLAUDE.md exists

先读取以下文件，建立核心上下文：
1. files/CLAUDE.md — 全量读取（产品定义 + 技术栈约束 + 编码规范 + 禁止事项，~6K tokens）
2. files/CONTEXT.md — 全量读取（当前 sprint 进度 + 流水线状态 + 已知问题 + 待确认决策，~5K tokens）
3. files/INTERFACES.md — 只读 §2.1 scan_repo, §2.4 ask_about（其余跳过，节约上下文空间）
4. docs/self_evolution_design.md — 只读 第四章：智能进化（增量扫描 + 隐式推断 + Hotspot）

📊 上下文预算：文档 ~18K + 源码 ~15K + 产出 ~15K + 执行 ~10K = ~58K tokens (29%) 🟢

【任务】流水线 D / 智能记忆 + 隐式推断
【所属】Wave 4（可与 A-4、C-2 并行）
【前置】D-2b + D-3 已完成

额外读取源码：
- mcp-server/src/glossary/term_resolver.py — 当前接口
- mcp-server/src/memory/project_memory.py — 当前接口
- mcp-server/src/tools/scan_repo.py — 理解当前扫描逻辑

1. 术语隐式推断（term_resolver.py 新增方法）：
   - infer_from_qa_history(repo_url) → list[TermEntry]
   - 从 QA 历史中提取用户业务词汇 ↔ 代码标识符关联
   - source=inferred, confidence=0.7
   - confidence < 0.8 不自动注入 prompt，标记 suggested

2. Hotspot 聚类（memory/interactions.py）：
   - detect_hotspots(repo_url) → list[Hotspot]
   - 规则：同模块被问 3+ 次，关键词重叠 > 50%
   - ask_about 上下文注入时引用 Hotspot

3. scan_repo 增量更新（scan_repo.py）：
   - 检查 ProjectMemory 是否有该 repo 的 context
   - 文件 SHA256 比对找变更
   - 变更 < 30% → 增量
   - 变更 >= 30% → 全量
   - 增量后跑依赖图一致性检查

4. SessionSummary 自动生成：
   - ProjectMemory.finalize_session()
   - 汇总 modules_explored, key_findings, unresolved_questions

5. 端到端验证：
   a. 扫描项目 → 问几个问题（产生 QA 记录）
   b. 检查是否生成了推断术语和 Hotspot
   c. 修改少量文件 → 再次 scan_repo → 验证走增量路径

═══ 质量自检清单（任务完成前逐项确认）═══

□ 1. 代码规范：所有新代码有类型注解 + structlog 日志 + 无 print
□ 2. 测试覆盖：新功能有正常路径 + 至少 1 个异常路径测试
□ 3. 回归验证：cd mcp-server && python -m pytest tests/ -x -q → 记录结果
□ 4. 接口一致：如修改了 tool 输入输出 → INTERFACES.md 已同步更新
□ 5. 错误处理：对外返回 {"status": "error", "error": str, "hint": str}
□ 6. 无硬编码：无 API Key / 模型名 / 绝对路径 硬编码
□ 7. 产出物完整：所有声称的产出文件确实存在且内容完整
□ 8. CONTEXT.md 已更新：追加任务日志（固定格式）
