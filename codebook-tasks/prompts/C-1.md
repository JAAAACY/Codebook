# Task: C-1 — 修复已知测试问题
# Wave: W1
# Project: CodeBook (mcp-server/)
# Working Dir: The repo root where files/CLAUDE.md exists

先读取以下文件，建立核心上下文：
1. files/CLAUDE.md — 全量读取（产品定义 + 技术栈约束 + 编码规范 + 禁止事项，~6K tokens）
2. files/CONTEXT.md — 全量读取（当前 sprint 进度 + 流水线状态 + 已知问题 + 待确认决策，~5K tokens）
3. files/INTERFACES.md — 只读 §2.5 codegen（其余跳过，节约上下文空间）

📊 上下文预算：文档 ~9K + 源码 ~15K + 产出 ~8K + 执行 ~10K = ~42K tokens (21%) 🟢

【任务】流水线 C / 修复已知测试问题
【所属】Wave 1（可与 A-1、D-1a 并行）
【前置】W0-1 对齐确认完成

目标：修复 I-001 + 激活 I-002 + 补充 codegen 边界测试

额外读取源码（任务需要，不算文档）：
- mcp-server/tests/ 目录结构 → 找到断言 4 tool 的测试
- 25 项跳过测试的 skip 原因

执行步骤：
1. 修复 test_mcp_server_has_four_tools → 断言 5 个 tool（I-001）
2. 分析 25 项 Conduit 集成测试跳过原因
   - 逐个检查 @pytest.mark.skip 的理由
   - 哪些可以用 mock 解决？哪些确实需要外部仓库？
3. 逐步激活：每激活一批跑一次 pytest 确认
4. 补充 codegen 边界测试（至少 5 个新用例）：
   - diff_valid = False 时的错误路径和错误信息
   - locate_result 和 file_paths 同时为空
   - 超大文件（>1000行）的 diff 生成
   - 无效 repo_path
   - instruction 为空字符串
5. 目标：0 skip（无法解决的记录详细原因）

═══ 质量自检清单（任务完成前逐项确认）═══

□ 1. 代码规范：所有新代码有类型注解 + structlog 日志 + 无 print
□ 2. 测试覆盖：新功能有正常路径 + 至少 1 个异常路径测试
□ 3. 回归验证：cd mcp-server && python -m pytest tests/ -x -q → 记录结果
□ 4. 接口一致：如修改了 tool 输入输出 → INTERFACES.md 已同步更新
□ 5. 错误处理：对外返回 {"status": "error", "error": str, "hint": str}
□ 6. 无硬编码：无 API Key / 模型名 / 绝对路径 硬编码
□ 7. 产出物完整：所有声称的产出文件确实存在且内容完整
□ 8. CONTEXT.md 已更新：追加任务日志（固定格式）
