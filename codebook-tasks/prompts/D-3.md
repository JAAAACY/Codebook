# Task: D-3 — 项目记忆持久化
# Wave: W3
# Project: CodeBook (mcp-server/)
# Working Dir: The repo root where files/CLAUDE.md exists

先读取以下文件，建立核心上下文：
1. files/CLAUDE.md — 全量读取（产品定义 + 技术栈约束 + 编码规范 + 禁止事项，~6K tokens）
2. files/CONTEXT.md — 全量读取（当前 sprint 进度 + 流水线状态 + 已知问题 + 待确认决策，~5K tokens）
3. files/INTERFACES.md — 只读 §2.3 diagnose, §2.4 ask_about（其余跳过，节约上下文空间）
4. docs/self_evolution_design.md — 只读 §3.4 记忆注入 + §3.5 交互层

📊 上下文预算：文档 ~16K + 源码 ~15K + 产出 ~15K + 执行 ~10K = ~56K tokens (28%) 🟢

【任务】流水线 D / 项目记忆持久化
【所属】Wave 3（可与 A-3、B-2a/b 并行）
【前置】D-1b 已完成

额外读取源码：
- mcp-server/src/tools/ask_about.py — 重点读 assemble_context() 和 DiagnosisCache
- mcp-server/src/tools/diagnose.py — 重点读返回值结构
- mcp-server/src/memory/project_memory.py — 公开 API

【并行注意】B-2b 同 Wave 改角色逻辑。D-3 只碰记忆读写，不碰 ROLE_CONFIG。

执行步骤：

1. 修改 diagnose.py：
   - 完成后调用 ProjectMemory.add_diagnosis()
   - 提取：module_name, query, diagnosis_summary, matched_locations

2. 修改 ask_about.py：
   - DiagnosisCache → 从 ProjectMemory 读写
   - assemble_context() 扩展 6→8 级：
     #1 目标模块 L3 摘要（必选）
     #2 目标模块源代码（必选）
     #3 上下游 1 跳 L3 摘要
     #4 持久化诊断结果（从 ProjectMemory）
     #5 用户批注（从 ProjectMemory）
     #6【新增】QA 历史摘要
     #7【新增】热点信息
     #8 上下游 2 跳 L3 摘要
   - 60K 字符预算不变

3. 修改 read_chapter.py：完成后 view_count += 1

4. 创建 src/tools/memory_feedback.py：
   新 MCP tool：module_name, question, answer_summary, confidence?
   → 写入 ProjectMemory.understanding.qa_history

5. 修改 server.py 注册 memory_feedback

6. 端到端验证：
   a. scan_repo → diagnose 一个问题 → 关闭进程
   b. 重新启动 → ask_about 同一模块 → 验证 context 中包含之前的诊断结果

═══ 质量自检清单（任务完成前逐项确认）═══

□ 1. 代码规范：所有新代码有类型注解 + structlog 日志 + 无 print
□ 2. 测试覆盖：新功能有正常路径 + 至少 1 个异常路径测试
□ 3. 回归验证：cd mcp-server && python -m pytest tests/ -x -q → 记录结果
□ 4. 接口一致：如修改了 tool 输入输出 → INTERFACES.md 已同步更新
□ 5. 错误处理：对外返回 {"status": "error", "error": str, "hint": str}
□ 6. 无硬编码：无 API Key / 模型名 / 绝对路径 硬编码
□ 7. 产出物完整：所有声称的产出文件确实存在且内容完整
□ 8. CONTEXT.md 已更新：追加任务日志（固定格式）
