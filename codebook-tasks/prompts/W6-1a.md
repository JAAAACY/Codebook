# Task: W6-1a — 跨线集成：术语 + 角色 + 记忆联动
# Wave: W6
# Project: CodeBook (mcp-server/)
# Working Dir: The repo root where files/CLAUDE.md exists

先读取以下文件，建立核心上下文：
1. files/CLAUDE.md — 全量读取（产品定义 + 技术栈约束 + 编码规范 + 禁止事项，~6K tokens）
2. files/CONTEXT.md — 全量读取（当前 sprint 进度 + 流水线状态 + 已知问题 + 待确认决策，~5K tokens）
3. files/INTERFACES.md — 只读 §2.1 scan_repo, §2.2 read_chapter, §2.3 diagnose, §2.4 ask_about（其余跳过，节约上下文空间）

📊 上下文预算：文档 ~13K + 源码 ~10K + 产出 ~5K + 执行 ~25K = ~53K tokens (26%) 🟢

【任务】跨线集成验证 — Part A（4 项联动冒烟测试）
【所属】Wave 6（W6-1a 先做，W6-1b 后做）
【前置】A-5, B-2b, D-4 全部完成
【子 session 说明】原 W6-1 拆为两个子 session。本 session 做联动冒烟。

用 fastapi-realworld-example-app 或 FastAPI 仓库：

1. 术语飞轮 + 角色系统联动：
   a. scan_repo（role=domain_expert, project_domain=如有）
   b. term_correct 纠正一个术语（如 "endpoint" → "接口地址"）
   c. read_chapter PM 视角 → 验证翻译使用了新术语
   d. read_chapter dev 视角 → 验证术语不被禁用

2. 项目记忆 + 诊断联动：
   a. diagnose 一个问题 → 记录诊断
   b. ask_about 同一模块 → 验证 context 包含历史诊断
   c. memory_feedback 回传答案摘要
   d. 再次 ask_about → 验证 QA 历史被引用

3. 增量扫描联动：
   a. 确认上面的 scan_repo 结果已缓存
   b. 不修改文件 → 再次 scan_repo → 验证走增量路径

4. Hotspot 验证：
   a. 对同一模块再问 2 个问题（累计 3+）
   b. 验证 Hotspot 出现

将前 4 项结果写入 test_results/integration_part_a.md

═══ 质量自检清单（任务完成前逐项确认）═══

□ 1. 代码规范：所有新代码有类型注解 + structlog 日志 + 无 print
□ 2. 测试覆盖：新功能有正常路径 + 至少 1 个异常路径测试
□ 3. 回归验证：cd mcp-server && python -m pytest tests/ -x -q → 记录结果
□ 4. 接口一致：如修改了 tool 输入输出 → INTERFACES.md 已同步更新
□ 5. 错误处理：对外返回 {"status": "error", "error": str, "hint": str}
□ 6. 无硬编码：无 API Key / 模型名 / 绝对路径 硬编码
□ 7. 产出物完整：所有声称的产出文件确实存在且内容完整
□ 8. CONTEXT.md 已更新：追加任务日志（固定格式）
