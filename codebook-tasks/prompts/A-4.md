# Task: A-4 — ask_about + codegen 压测
# Wave: W4
# Project: CodeBook (mcp-server/)
# Working Dir: The repo root where files/CLAUDE.md exists

先读取以下文件，建立核心上下文：
1. files/CLAUDE.md — 全量读取（产品定义 + 技术栈约束 + 编码规范 + 禁止事项，~6K tokens）
2. files/CONTEXT.md — 全量读取（当前 sprint 进度 + 流水线状态 + 已知问题 + 待确认决策，~5K tokens）
3. files/INTERFACES.md — 只读 §2.4 ask_about, §2.5 codegen（其余跳过，节约上下文空间）

📊 上下文预算：文档 ~10K + 源码 ~10K + 产出 ~15K + 执行 ~20K = ~55K tokens (28%) 🟢

【任务】流水线 A / ask_about + codegen 压力测试
【所属】Wave 4（可与 C-2、D-4 并行）
【前置】A-3 已完成

关键架构确认（读 INTERFACES.md 后二次确认）：
- ask_about 不内部调用 LLM，返回 context + guidance，由宿主推理
- codegen 输出含 diff_valid 和 validation_detail

对最大的成功项目：

ask_about 测试：
1. 选 1 个核心业务模块
2. PM 视角 3 轮对话（传 conversation_history）：
   - "这个模块是做什么的？"
   - "加新功能 {feature} 需要改哪里？"
   - "改完怎么验证不影响其他功能？"
3. 每轮记录：
   - context 长度 + context_modules_used
   - guidance 是否与 role 匹配
   - context 中是否有足够信息回答问题
   - 是否引用不存在的文件/函数（幻觉检查）
   - 翻译质量 1-10 + 扣分原因

codegen 测试：
4. 设计 1 个多文件修改需求
5. diagnose 定位 → locate_result → codegen
6. 记录：
   - diff_valid + validation_detail
   - unified_diff 语法正确性（能否 git apply）
   - change_summary 每项 before/after 准确性
   - blast_radius 是否遗漏影响
   - verification_steps 每步是否可执行

═══ 质量自检清单（任务完成前逐项确认）═══

□ 1. 代码规范：所有新代码有类型注解 + structlog 日志 + 无 print
□ 2. 测试覆盖：新功能有正常路径 + 至少 1 个异常路径测试
□ 3. 回归验证：cd mcp-server && python -m pytest tests/ -x -q → 记录结果
□ 4. 接口一致：如修改了 tool 输入输出 → INTERFACES.md 已同步更新
□ 5. 错误处理：对外返回 {"status": "error", "error": str, "hint": str}
□ 6. 无硬编码：无 API Key / 模型名 / 绝对路径 硬编码
□ 7. 产出物完整：所有声称的产出文件确实存在且内容完整
□ 8. CONTEXT.md 已更新：追加任务日志（固定格式）
