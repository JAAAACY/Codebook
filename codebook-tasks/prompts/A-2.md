# Task: A-2 — scan_repo 压力测试
# Wave: W2
# Project: CodeBook (mcp-server/)
# Working Dir: The repo root where files/CLAUDE.md exists

先读取以下文件，建立核心上下文：
1. files/CLAUDE.md — 全量读取（产品定义 + 技术栈约束 + 编码规范 + 禁止事项，~6K tokens）
2. files/CONTEXT.md — 全量读取（当前 sprint 进度 + 流水线状态 + 已知问题 + 待确认决策，~5K tokens）
3. files/INTERFACES.md — 只读 §2.1 scan_repo（其余跳过，节约上下文空间）

📊 上下文预算：文档 ~8K + 源码 ~5K + 产出 ~15K + 执行 ~20K = ~48K tokens (24%) 🟢

【任务】流水线 A / scan_repo 压力测试
【所属】Wave 2（可与 B-1、D-2a 并行）
【前置】A-1 已完成

对 4 个项目从小到大逐个运行 scan_repo（overview 模式）。
崩溃时记录完整 traceback 后继续下一个。

对每个项目记录到 test_results/{name}/scan_repo.json：
{
  "性能": {"scan_time_seconds": N, "step_times": {...}, "memory_peak_mb": N, "total_lines": N},
  "规模": {"modules": N, "functions": N, "classes": N, "imports": N, "calls": N, "languages": {...}},
  "质量": {
    "mermaid_nodes": N,
    "mermaid_readable": "1-10分 + 具体说明为什么扣分",
    "health_distribution": {"green": N, "yellow": N, "red": N},
    "module_grouping_score": "1-10分 + 哪些模块分得不合理",
    "error": null | "完整错误信息",
    "edge_cases": "遇到的边界情况描述"
  }
}

再用 detailed 模式运行一次中型项目，记录 chapters 差异和耗时差异。

全部测完后写 test_results/scan_repo_summary.md：
- 四个项目的横向对比表
- 性能与项目规模的关系（是否线性增长）
- 质量随规模变化的趋势
- 发现的所有问题和瓶颈（按严重程度排序）

═══ 质量自检清单（任务完成前逐项确认）═══

□ 1. 代码规范：所有新代码有类型注解 + structlog 日志 + 无 print
□ 2. 测试覆盖：新功能有正常路径 + 至少 1 个异常路径测试
□ 3. 回归验证：cd mcp-server && python -m pytest tests/ -x -q → 记录结果
□ 4. 接口一致：如修改了 tool 输入输出 → INTERFACES.md 已同步更新
□ 5. 错误处理：对外返回 {"status": "error", "error": str, "hint": str}
□ 6. 无硬编码：无 API Key / 模型名 / 绝对路径 硬编码
□ 7. 产出物完整：所有声称的产出文件确实存在且内容完整
□ 8. CONTEXT.md 已更新：追加任务日志（固定格式）
